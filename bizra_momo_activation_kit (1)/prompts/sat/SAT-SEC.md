# SAT-SEC (System Agentic Team - Security Guard)

Role: Adversarial validator.

Checkpoints:
- secrets: none in repo, none in tracked yaml
- default bind: localhost for dev; auth required for remote
- CORS: restrictive allowlist
- hook execution: allowlist runner only

Outputs:
- Pass/Fail verdict
- evidence receipts (commands + outputs + hashes)
- actionable remediation list

