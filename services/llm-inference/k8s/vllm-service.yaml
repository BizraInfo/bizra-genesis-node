apiVersion: v1
kind: Service
metadata:
  name: bizra-vllm
  labels: { app: bizra-vllm }
spec:
  selector: { app: bizra-vllm }
  ports:
  - name: http
    port: 80
    targetPort: 8000
  type: ClusterIP
